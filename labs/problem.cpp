#include <iostream>
#include <string>

using namespace std;

int main(){
	string oldstr, newstr;
	int count = 0;
	cout << "Please enter a string with only letters: ";
	cin >> str;
	while(count < oldstr.length()){
		char letter = 'z';
		for(i = 0; i < oldstr.length(); i++){
			if(oldstr.at(i) <= letter){
				letter = oldstr.at(i);
			}
		}
		for(i = 0; i < oldstr.length(); i++){
			if(oldstr.at())
		}
	}
}